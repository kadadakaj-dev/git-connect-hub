# Príkazy pre terminál

Tu sú presné príkazy, ktoré som spustil v termináli, vrátane absolútnych ciest:

1. Príkaz na Build
Tento príkaz pripraví produkčnú verziu aplikácie:

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm build
2. Spustenie Dev Servera na porte 3000
Tento príkaz spustí vývojový server na predvolenom porte:

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm dev --port 3000
3. Spustenie Dev Servera na porte 4000
Tento príkaz spustí druhú inštanciu vývojového servera na porte 4000:

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm dev --port 4000
Oba servery sú momentálne aktívne a bežia na pozadí. Ak by ste potrebovali niektorý z nich reštartovať, môžete použiť tieto príkazy.

na build a na dev server

Thought for <1s

Tu sú presné príkazy s cestou, ktoré môžete skopírovať priamo do terminálu:

Pre Build:

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm build
Pre Dev Server (Port 3000):

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm dev --port 3000
Pre Dev Server (Port 4000):

```powershell
cd "c:\Users\engli\Documents\BookingPro - Larsen Evans Enterprise\zaloha-booking\booking-pro-LE-main\web"; pnpm dev --port 4000
Oba servery (3000 aj 4000) som už pre vás spustil a momentálne bežia. Ak ich chcete spustiť znova manuálne, použite tieto príkazy.
